import React from "react";
import {ActionFormProps} from "@/components/script/typings";


export const ReturnActionForm:React.FC<ActionFormProps> = (props)=>{

    return (
        <>
            return
        </>
    )
}